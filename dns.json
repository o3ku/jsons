{
  "dns": {
    "servers": [
          { "tag": "dns_proxy", "address": "https://223.5.5.5/dns-query", "strategy": "ipv4_only", "address_resolver": "dns_resolver" },
          { "tag": "dns_direct", "address": "https://223.5.5.5/dns-query", "strategy": "ipv4_only", "address_resolver": "dns_resolver", "detour": "DIRECT" },
          { "tag": "dns_fakeip", "address": "fakeip" },
          { "tag": "dns_resolver", "address": "223.5.5.5", "detour": "DIRECT" },
          { "tag": "block", "address": "rcode://success" },
          { "tag": "local", "address": "local", "detour": "DIRECT" },
          { "tag": "dns_dnsmasq", "address": "127.0.0.1", "detour": "DIRECT" }
        ],
    "rules": [
          { "outbound": ["any"], "server": "dns_direct" },
          { "clash_mode": "Global", "server": "dns_fakeip", "rewrite_ttl": 1 },
          { "clash_mode": "Direct", "server": "dns_direct" },
          {
                "domain_suffix": [
                    "local",
                    "localdomain",
                    "example",
                    "invalid",
                    "localhost",
                    "test",
                    "local",
                    "home.arpa",
                    "scfypt.com"
                ],
                "server": "dns_direct"
          },
          {
                "domain_suffix": [
                    "lan",
                    "work.gd",
                    "ftp.sh",
                    "ydns.eu",
                    "dns.army"
                ],
                "server": "dns_dnsmasq"
          },
          { "query_type": [ "A", "AAAA" ], "server": "dns_fakeip", "rewrite_ttl": 1 }
        ],
    "final": "dns_proxy",
    "independent_cache": true,
    "reverse_mapping": true,
    "fakeip": { "enabled": true, "inet4_range": "198.18.0.0/16", "inet6_range": "fc00::/16" }
  }
}
